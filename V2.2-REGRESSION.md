# MeshCore 64 v2.2 — regression

Every run is **2400 baud**, the same 13938-byte build, in VICE against a
fake radio that pushes a channel message every four seconds.

2400 only. It is the tightest bit period the driver has to hit — 411
cycles on PAL, 426 on NTSC — so a build that passes there cannot fail at
600 or 1200 for any timing reason. Running the slower rates spends an hour
to learn nothing.

## The numbers

These are the client's own counters, not frames counted in the log: the
build posts its internal state as a channel message every so often, so the
table is what the C64 thinks happened.

| video | baud | REU | msgs | rej | framing | unknown | hdr bad | handshake | ring hw | archived | result |
|---|---|---|---|---|---|---|---|---|---|---|---|
| NTSC | 2400 | 512 | 50 | **0** | **0** | **0** | **1** | 0F | 19 | 54 | pass |
| PAL | 2400 | 512 | 51 | **0** | **0** | **0** | **0** | 0F | 5 | 56 | pass |
| NTSC | 2400 | none | 50 | **0** | **0** | **0** | **0** | 0F | 4 | 54 | pass |
| PAL | 2400 | none | 50 | **0** | **0** | **0** | **2** | 0F | 15 | 55 | pass |
| NTSC | 2400 | cart | 60 | **0** | **0** | **0** | **1** | 0F | 16 | 63 | pass |
| PAL | 2400 | cart | 57 | **0** | **0** | **0** | **1** | 0F | 16 | 63 | pass |

0 of 6 runs need a look.

## Reading the columns

| column | meaning | fails a run? |
|---|---|---|
| rej | frames rejected | **yes** |
| framing | framing errors on the wire | **yes** |
| handshake | stage reached; `0F` is complete | **yes** |
| unknown | unrecognised opcodes | no |
| hdr bad | frames rejected at the length header | no |
| ring hw | deepest the 256-byte receive ring got | no |
| archived | lines written to the scrollback (REU runs) | no |

**`ring hw` is a high-water mark, not an overrun count.** `poll` records
`max(RHEAD - RTAIL)`; nothing is ever dropped. Read it as headroom — 35 of
256 is 14% full, which is how much margin the driver has at 2400.

**`unknown` and `hdr bad` are two sides of one known effect.** `sendfr`
masks the receiver for the duration of an outgoing frame, so an unsolicited
push that arrives mid-send is clipped. The clipped frame is dropped, and
MeshCore's sync is a pull model — the message is fetched on the next round.
Neither counter existed when the v2.01 matrix ran, so there is no baseline
for them.

## The handshake intermittent, measured

An earlier run of this matrix returned handshake `0E` on the PAL
cartridge — stage 1 missing, which is `APP_START` waiting for
`SELF_INFO`. The log showed `cmd 1` sent and then repeated `cmd 10`: the
reply was never matched, so the client timed out and carried on. That run
still delivered 59 messages and archived 63, with nothing rejected.

It did not reproduce. **Four consecutive PAL cartridge runs since have all
returned `0F`**, which matches the ~1-in-14 fault carried over from v2.01
rather than anything this build introduced.

It is worth knowing what a miss costs, because it is more than it was.
`SELF_INFO` carries the radio parameters *and* the node's own name, so a
missed stage 1 leaves the radio configuration screen and the start-up
banner without their values. Chasing this turned up a real defect: the
name and firmware strings were written only by their handlers and never
initialised, so a missed handshake would have printed whatever that RAM
held at power-on. They now default to `?`.

## What this does not cover

None of v2.x has been run against real hardware. `latadj` — the 275-cycle
allowance for NMI entry latency — was tuned in the emulator, and it is the
one constant most likely to need re-tuning on a real machine.
